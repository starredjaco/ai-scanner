#!/usr/bin/env sh

if gem list --no-installed --exact --silent foreman; then
  echo "Installing foreman..."
  gem install foreman
fi

# Default to port 80 if not specified
export PORT="${ENV_PORT:-${PORT:-80}}"

exec foreman start -f Procfile.dev --env /dev/null "$@"
